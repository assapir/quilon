~ Double recursion. `<< core.test` verifies the result; on success the program exits 0.
<< core.test

fib = (n :: Num) -> Num => < n <= 1 ? n : fib(n - 1) + fib(n - 2) >

^ = () -> $ => <
  assert(fib(10), equals(55))
  assert(fib(1), equals(1))
>
