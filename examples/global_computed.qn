~ A computed top-level binding: its initializer is an operator expression, run once before
~ `^`, using a global defined above it. See `examples/globals.qn` for the full range of
~ top-level forms.
<< core.test

base = 10
doubled = base * 2

^ = () -> $ => <
  assert(doubled, equals(20))
>
