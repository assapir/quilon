~ Test fixture: an exported COMPUTED `=` binding — its value comes from a
~ function defined above it, not a literal. `>>` on an `=` binding still exports; only a
~ `:=` (mutable) global rejects `>>`.

countBirds = (n :: Num) -> Num => < n * 2 >

>> penguins = countBirds(3)
