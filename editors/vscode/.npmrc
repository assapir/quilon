# Hoist transitive dependencies flat into node_modules (instead of pnpm's
# default symlinked-store layout) so @vscode/vsce can walk a conventional
# dependency tree when building the .vsix. Without this, vsce cannot resolve
# the full tree through pnpm's symlinks and packaging breaks.
node-linker=hoisted
