~ Interpolation in a loop: each step renders two values and measures the result.
build = (n :: Num, acc :: Num) -> Num => < n == 0 ? acc : build(n - 1, acc + "item `n` of `n * 2`".size) >
^ = () -> Num => < build(400000, 0) > 0 ? 0 : 1 >
